1. STATUS GERAL DA FASE 1
- Status: Parcialmente concluída.
- Percentual aproximado: 80%.

2. ITENS JÁ IMPLEMENTADOS
- COUNT com filtros corrigido
  - O que foi feito: as consultas de listagem passaram a construir um WHERE dinâmico e o mesmo WHERE é usado na query de COUNT.
  - Arquivos:
    - backend/src/modules/mensalidades/repositories/mensalidadeRepository.js
    - backend/src/modules/agendamentos/repositories/agendamentoRepository.js
    - backend/src/modules/profissionais/repositories/profissionalRepository.js
    - backend/src/modules/horarios-aula/repositories/horarioAulaRepository.js
    - backend/src/modules/modalidades/repositories/modalidadeRepository.js
    - backend/src/modules/presencas/repositories/presencaRepository.js
    - backend/src/modules/cts/repositories/ctRepository.js
    - backend/src/modules/transacoes/repositories/transacaoRepository.js (verificado)
  - Como: construção de `filtros` / `params`, criação de `where` comum para SELECT com LIMIT/OFFSET e para `SELECT COUNT(*) ...` com os mesmos parâmetros.
  - Testes: adição de asserts em backend/tests/integration/mensalidades.spec.js que validam que `total` corresponde ao conjunto filtrado.

- Race condition: uso de UNIQUE mapping via ER_DUP_ENTRY (parcial)
  - O que foi feito: para o fluxo de mensalidades o serviço captura ER_DUP_ENTRY e converte em 409; o errorHandler ganhou mapeamento para a constraint `uq_mensalidade_aluno_competencia`.
  - Arquivos:
    - backend/src/modules/mensalidades/services/mensalidadeService.js
    - backend/src/shared/middlewares/errorHandler.js
  - Como: confiar no banco ao capturar ER_DUP_ENTRY ao inserir/atualizar; manter validação preemptiva no serviço.
  - Testes: testes de mensalidades cobrem duplicidade e passaram.

- Transações em operações multi-step
  - O que foi feito: fluxo de criação de agendamento foi alterado para executar dentro de transação quando `horario.limite_vagas` está definido (getConnection, beginTransaction, re-checar duplicidade, contar vagas, inserir, commit/rollback).
  - Arquivos:
    - backend/src/modules/agendamentos/services/agendamentoService.js
    - backend/src/modules/agendamentos/repositories/agendamentoRepository.js (aceita conexão `conn` opcional)
  - Como: execução dos passos críticos na mesma conexão transacional para evitar TOCTOU quando `limite_vagas` está definido.
  - Testes: teste transacional adicionado em backend/tests/integration/agendamentos.spec.js (falhando — ver item 4).

- Seeds corrigidos (sem IDs fixos)
  - O que foi feito: mysql-init/01-init.sql modificado para inserts idempotentes (INSERT ... SELECT ... WHERE NOT EXISTS) e referências por subqueries.
  - Arquivos: mysql-init/01-init.sql
  - Como: checks WHERE NOT EXISTS para evitar duplicação em re-runs do script.

- Docker de teste ajustado
  - O que foi feito: removido container_name fixo e volume nomeado persistente em docker-compose.test.yml para melhorar isolamento.
  - Arquivos: docker-compose.test.yml
  - Como: usar volumes efêmeros e evitar nome fixo.

3. ITENS PENDENTES
- Race condition / duplicidade em `agendamentos`
  - O que falta: adicionar constraint UNIQUE em `agendamentos` (account_id, aluno_id, horario_aula_id, data_aula) e capturar ER_DUP_ENTRY na criação.
  - Por que não foi feito: necessidade de alteração de schema; decisão não aplicada ainda para evitar migração sem limpeza de dados.
  - Complexidade: média.
  - Risco: alto — duplicidade possível sob concorrência.

- Falha do teste transacional (limite_vagas)
  - O que falta: investigar por que o teste mostrou 0 agendamentos no filtro (esperado 1). Verificar commit, queries e assert.
  - Por que não foi resolvido: exigirá depuração com logs/execução isolada.
  - Complexidade: média.
  - Risco: médio — fluxo transacional pode não estar efetivando o commit corretamente em todos os cenários.

- Tratamento ER_DUP_ENTRY em agendamentoRepository.criar (dependente de UNIQUE)
  - O que falta: capturar ER_DUP_ENTRY e mapear para AppError(409).
  - Por que não foi feito: sem UNIQUE o banco não gera ER_DUP_ENTRY.
  - Complexidade: baixa.
  - Risco: médio enquanto UNIQUE não existir.

- Migração DDL segura para produção
  - O que falta: preparar migration script e plano de execução (detectar/limpar duplicados antes de criar UNIQUE).
  - Complexidade: média-alta.

4. PROBLEMAS ENCONTRADOS DURANTE A IMPLEMENTAÇÃO
- Dificuldades técnicas:
  - Ausência de UNIQUE em `agendamentos` impede garantia absoluta contra duplicidade; transação protege parcialmente, mas não substitui constraint de banco.
  - Teste transacional falhou — precisa de debug (possíveis causas: commit não executado, criação do horário sem limite_vagas, ou filtro/asserção incorreta).
- Impacto em testes existentes:
  - 2 testes falharam na suite `agendamentos` (duplicidade e contagem transacional). As demais 10 suites passaram.
- Decisões ambíguas:
  - Optamos por transação apenas quando `limite_vagas` definido; essa decisão deixou lacuna de proteção para o caso geral de duplicidade.

5. TESTES
- Testes criados/alterados:
  - Alterado: backend/tests/integration/mensalidades.spec.js (asserts adicionais para `total`, remoção de duplicação de bloco).
  - Criado: backend/tests/integration/agendamentos.spec.js (teste: limite_vagas e atomicidade).
- O que garantem:
  - Mensalidades: `total` filtrado consistente, duplicidade tratada.
  - Agendamentos (novo): quando `limite_vagas=1`, somente 1 agendamento deve existir; tentativa adicional deve retornar 409.
- Estado atual:
  - Execução real: 11 suítes, 10 passaram, 1 suite falhou; 82 testes executados, 80 passaram, 2 falharam (ambos em agendamentos).
- Lacunas de cobertura:
  - Falta teste que simule concorrência real (requests paralelos) para reproduzir TOCTOU.
  - Falta captura de ER_DUP_ENTRY para `agendamentos` (só possível após UNIQUE no DB).

6. RISCOS ATUAIS
- Agendamentos podem ficar duplicados sob concorrência (risco funcional direto).
- Fluxos com `limite_vagas` podem não estar protegidos se transação não estiver commitando corretamente (observado nos testes).
- As áreas corrigidas (mensalidades, listagens, seeds, docker test) estão com risco baixo.

7. PRÓXIMOS PASSOS PARA FINALIZAR A FASE 1
1) Adicionar constraint UNIQUE em `agendamentos` (DDL + migration idempotente).
2) Capturar ER_DUP_ENTRY em `agendamentoRepository.criar` e mapear para AppError(409).
3) Re-executar e depurar os testes de `agendamentos` (rodar com logs SQL/commit/rollback).
4) Preparar migration script + instruções para dados existentes (detecção/remediação de duplicados).
5) Reexecutar suíte completa e confirmar 100% verde.

Observação final:
- O grosso da Fase 1 foi implementado conforme escopo; resta a ação crítica em `agendamentos` (adicionar UNIQUE e tratar ER_DUP_ENTRY) e depurar a falha do teste transacional. Se autorizar, procedo com a implementação DDL+tratamento (ou preparo PR com instruções).